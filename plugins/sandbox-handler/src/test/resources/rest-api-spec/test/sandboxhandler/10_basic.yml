"Test that the sandbox-handler plugin is loaded in Elasticsearch":

  # Use the Cat Plugins API to retrieve the list of plugins
  - do:
      cat.plugins:
        local: true
        h: component

  - match:
      $body: /^sandbox-handler\n$/
